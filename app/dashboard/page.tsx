export default function Page(){
  return <p>Dashboard Page</p>
}
//to create roots in nextjs projects create folders with the url segments and add a page.tsx file inside it
//the page.tsx file is a special file that contains a ui component (server component)
//and that file should be defualt exported that tells nextjs what thing to render
//because we can have a lot of things but the thing being defualt export is rendered
